# SpringBoot 配置文件、配置绑定、自定义配置（以阿里云 OSS 举例）

## 1. 什么是配置文件

SpringBoot 的配置文件，就是**外部化配置文件**，用来放可变参数，避免硬编码写死在 Java 代码。两种格式：

- `application.yml`（主流，苍穹外卖用这个）
- `application.properties`

里面写数据库地址、端口、oss 密钥、超时时间等。

> 好处：修改配置，不用改 Java 代码、不用重新编译项目，直接改配置文件重启即可。

示例 oss 配置片段（yml）

```
aliyun:
  oss:
    endpoint: oss-cn-beijing.aliyuncs.com
    access‑key‑id: LTAIxxxx
    access‑key‑secret: abc123456
    bucket‑name: my‑bucket
```

## 2. 配置文件为什么会生效？

SpringBoot 启动流程：

1. SpringBoot 启动的时候，会**自动加载application.yml / application.properties**，把文件里面全部键值对读到内存，存入一个叫 `Environment` 的环境对象。

> `Environment` 里面存放全部外部配置：配置文件、系统环境变量、JVM 启动参数等。

1. 框架各个组件，需要参数的时候，就从`Environment`里面读取配置。例子：SpringBoot 的数据源自动配置类，会去 Environment 读取 `spring.datasource.url`、username、password，创建 DataSource。

> 本质：配置文件加载进 Environment，框架从 Environment 拿值。

> 优先级：配置文件 < JVM 参数 < 系统环境变量（后面的会覆盖前面）

## 3. 配置映射类 @ConfigurationProperties（重点，映射配置）

> 问题：配置文件里面一堆自定义配置（aliyun.oss.xxx），如果到处用`@Value("${aliyun.oss.endpoint}")`，代码会非常散乱。

SpringBoot 提供**配置绑定**：把 yml/properties 中的一组配置，**自动映射绑定到一个 Java 实体类的成员变量**，这个类叫**配置映射类**。

核心注解：`@ConfigurationProperties(prefix = "aliyun.oss")`

> prefix = 配置文件的前缀，yml 中前缀匹配上，同名字段自动赋值。

### 完整 OSS 配置映射类示例

```
import org.springframework.boot.context.properties.ConfigurationProperties;

/**
 * 阿里云OSS配置映射类
 * 读取yml中 aliyun.oss 开头的全部配置，自动赋值给成员变量
 */
@ConfigurationProperties(prefix = "aliyun.oss")
public class AliOssProperties {
    // 字段名和yml的key名字保持一致，自动注入
    private String endpoint;
    private String accessKeyId;
    private String accessKeySecret;
    private String bucketName;

    //必须要有 set方法！！！配置绑定底层通过setter赋值，没有set无法映射
    //getter setter
    public String getEndpoint() { return endpoint; }
    public void setEndpoint(String endpoint) { this.endpoint = endpoint; }
    public String getAccessKeyId() { return accessKeyId; }
    public void setAccessKeyId(String accessKeyId) { this.accessKeyId = accessKeyId; }
    public String getAccessKeySecret() { return accessKeySecret; }
    public void setAccessKeySecret(String accessKeySecret) { this.accessKeySecret = accessKeySecret; }
    public String getBucketName() { return bucketName; }
    public void setBucketName(String bucketName) { this.bucketName = bucketName; }
}
```

> ⚠️关键点：

1. yml 配置 key 和 Java 类的属性名**对应**。yml 里`access‑key‑id` 自动映射 Java 驼峰 `accessKeyId`，横杠转驼峰是 SpringBoot 自动支持。
2. **必须提供 setter 方法！** 配置绑定底层调用 set 方法给属性赋值；lombok 的 @Data 可以直接生成 get/set。
3. `@ConfigurationProperties`本身**不会把这个类交给 IOC 容器**。

### 两种开启绑定的方式（二选一）

#### 方式 1：在配置映射类上加 `@Component`

```
@Component
@ConfigurationProperties(prefix = "aliyun.oss")
public class AliOssProperties {
}
```

把配置类变成 IOC 组件。之后业务代码直接`@Autowired AliOssProperties properties`注入使用。

#### 方式 2：在**启动类 / 配置类**上加注解 `@EnableConfigurationProperties(AliOssProperties.class)`（苍穹外卖推荐）

```
@SpringBootApplication
@EnableConfigurationProperties(AliOssProperties.class)
public class SkyApplication {
    public static void main(String[] args) {
        SpringApplication.run(SkyApplication.class, args);
    }
}
```

> 作用：告诉 SpringBoot，启用这个配置绑定类，自动完成绑定并且注册进 IOC 容器。好处：配置映射类职责纯粹，只做配置接收，不写`@Component`，解耦。

> 不写上面任意一种：配置不会绑定，所有字段都是 null！

## 4. 底层原理：为什么配置文件的值能自动赋值给 Java 类？

1. yml 全部配置加载进`Environment`。
2. `@EnableConfigurationProperties`或者`@Component`让这个类进入 Spring 处理流程。
3. SpringBoot 内置`ConfigurationPropertiesBindingPostProcessor`**后置处理器**。
4. 后置处理器拿到这个 Bean，读取注解上的`prefix`。
5. 从`Environment`取出所有以该前缀开头的配置项。
6. 通过**反射调用 setter 方法**，把配置的值设置给 Java 对象的成员变量。

> ❗不是直接访问 private 私有字段！是调用 set 方法，所以没有 setter 就绑定失败。

对比`@Value`：

- `@Value("${aliyun.oss.endpoint}")`：单个配置项注入。适合少量配置。
- `@ConfigurationProperties`：批量绑定一组前缀下所有配置，适合自定义组件（OSS、第三方 SDK）。

## 5. 结合 @Bean，完整自定义阿里云 OSS 组件（完整流程）

> 需求：读取 yml 的 oss 配置，创建 OSS 客户端对象交给 IOC 容器。

### 第一步：pom 引入阿里云 oss 依赖

```
<dependency>
    <groupId>com.aliyun.oss</groupId>
    <artifactId>aliyun-sdk-oss</artifactId>
    <version>3.17.0</version>
</dependency>
```

### 第二步：application.yml 写自定义配置

```
aliyun:
  oss:
    endpoint: oss-cn-beijing.aliyuncs.com
    access‑key‑id: LTAIxxxx
    access‑key‑secret: abc123456
    bucket‑name: sky‑bucket‑demo
```

### 第三步：编写配置映射类 AliOssProperties

```
import lombok.Data;
import org.springframework.boot.context.properties.ConfigurationProperties;

@Data //lombok生成get、set
@ConfigurationProperties(prefix = "aliyun.oss")
public class AliOssProperties {
    private String endpoint;
    private String accessKeyId;
    private String accessKeySecret;
    private String bucketName;
}
```

### 第四步：写配置类，注册 OSS 客户端 Bean

```
import com.aliyun.oss.OSS;
import com.aliyun.oss.OSSClientBuilder;
import org.springframework.boot.context.properties.EnableConfigurationProperties;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;

@Configuration
@EnableConfigurationProperties(AliOssProperties.class) //开启配置绑定，AliOssProperties放入IOC
public class OssConfig {

    // Spring会自动把已经绑定好配置的AliOssProperties注入进来
    @Bean
    public OSS ossClient(AliOssProperties aliOssProperties){
        // 使用配置映射类里面读取到的值，创建OSS客户端对象
        return new OSSClientBuilder()
                .build(
                        aliOssProperties.getEndpoint(),
                        aliOssProperties.getAccessKeyId(),
                        aliOssProperties.getAccessKeySecret()
                );
    }
}
```

### 第五步：业务层直接使用

```
@Service
public class FileService {
    @Autowired
    private OSS ossClient;

    public void uploadFile(){
        //直接使用ossClient，配置全部来自yml
    }
}
```

## 6. 整体流程串讲

1. `application.yml`写自定义 aliyun.oss 配置。
2. SpringBoot 启动，加载 yml 全部键值对存入`Environment`。
3. `@EnableConfigurationProperties(AliOssProperties.class)`，开启绑定。
4. 后置处理器读取 Environment 中`aliyun.oss`开头配置，调用 set 方法赋值给 AliOssProperties 对象，放入 IOC 容器。
5. OssConfig 配置类中`@Bean`方法，Spring 注入已经填好数据的 AliOssProperties。
6. 使用配置里面的属性，new OSS 客户端，把客户端交给 IOC 容器。
7. Service 直接注入 OSS 客户端，完成开发。

## 7. 高频坑

1. 忘记 setter 方法：字段全部 null，用`@Data`避免。
2. yml key 名称和 Java 属性不匹配，横杠`access‑key‑id`对应驼峰`accessKeyId`，Spring 自动转换。
3. 忘记`@EnableConfigurationProperties`，也不加`@Component`：配置映射类不会绑定，全部 null。
4. yml 缩进错误，层级错了，前缀匹配失败。

## 面试问答总结

> Q：`@ConfigurationProperties`的原理？A：配置文件加载到 Environment；`ConfigurationPropertiesBindingPostProcessor`后置处理器，根据 prefix，从 Environment 取出对应配置，反射调用 setter 方法给对象赋值。

> Q：`@Value`和`@ConfigurationProperties`区别？A：@Value 单个注入；@ConfigurationProperties 批量绑定一组配置，适合第三方组件自定义配置。

> Q：自定义第三方组件配置怎么做？

1. yml 写自定义配置；
2. 创建配置映射类，`@ConfigurationProperties(prefix="xxx")`；
3. `@EnableConfigurationProperties`开启绑定；
4. 在配置类`@Bean`，使用绑定完成的 properties 对象，创建第三方 SDK 对象交给 IOC。

> Q：配置文件为什么生效？A：SpringBoot 启动加载 yml/properties，全部放入 Environment 环境，框架、配置绑定都从 Environment 读取参数。